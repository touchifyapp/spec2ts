{
  "version": "independent",
  "command": {
    "version": {
      "allowBranch": ["master", "@release/*"],
      "conventionalCommits": true,
      "message": "chore(release): publish packages",
      "ignoreChanges": [
        "**/*.md",
        "**/LICENSE",
        "**/tests/**"
      ]
    },
    "bootstrap": {
      "hoist": "**"
    }
  },
  "packages": [
    "packages/*"
  ]
}